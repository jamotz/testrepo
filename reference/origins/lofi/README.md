# Drop your lo-fi frames here
Low-fidelity wireframes / early sketches for Origins (name them by screen,
e.g. `home-wire.png`, `shop-wire.png`, `filters-wire.png`). These pair with
the hi-fi frames to show the wireframe -> final progression in the case study.
