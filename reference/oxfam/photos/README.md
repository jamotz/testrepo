# oxfam/photos/ — general photos for the build

Stock photos, imagery, or other general visual assets to use throughout the
wizard build (not screenshots of a specific page/flow step — those go in
`lofi/` or the `page-0X/` folders). Logos go here too unless you'd rather
keep using `process/`.
